import 'package:flutter/material.dart';

class AppConstants {
  static const appName = 'Attendance Tracker';

  // Define other constants here
  static const primaryColor = Colors.blue;
  static const accentColor = Colors.blueAccent;
}